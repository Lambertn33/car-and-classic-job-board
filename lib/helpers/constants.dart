import 'package:flutter/material.dart';

class Constants {
  static Color primaryColor =
      Color(int.parse('#3b5998'.substring(1, 7), radix: 16) + 0xFF000000);
  static Color lightColor = Colors.white;
  static const primaryFont = 'Poppins';
}
